name: Setup GitHub Pages

on:
  workflow_dispatch:

jobs:
  setup-pages:
    name: Setup GitHub Pages
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
      contents: read
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v4